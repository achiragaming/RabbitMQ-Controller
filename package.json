{
  "name": "rabbitmq-manager",
  "version": "1.0.0",
  "description": "The RabbitMQManager is a TypeScript Library that provides a convenient way to manage RabbitMQ connections with features like automatic reconnection and error handling. It extends the `EventEmitter` class to emit events for various connection states.",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "url": "https://github.com/NoErrorStudio/RabbitMQ-Controller"
  },
  "keywords": [
    "rabbitmq",
    "amqplib",
    "rabbitmq-controller"
  ],
  "author": "achira",
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.10.3"
  }
}
