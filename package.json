{
  "name": "node-rabbitmq-manager",
  "version": "1.0.9",
  "description": "The RabbitMQManager is a TypeScript Library that provides a convenient way to manage RabbitMQ connections for Nodejs with features like automatic reconnection and error handling. It extends the `EventEmitter` class to emit events for various connection states.",
  "main": "dist/index.js",
  "types": "types/node-rabbitmq-manager.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "repository": {
    "url": "https://github.com/NoErrorStudio/Node-RabbitMQ-Manager"
  },
  "keywords": [
    "rabbitmq",
    "amqplib",
    "rabbitmq-manager",
    "node-rabbitmq-manager"
  ],
  "author": "achira",
  "type": "module",
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.10.3"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.3"
  }
}
