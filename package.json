{
  "name": "rabbitmq-controller",
  "version": "2.1.0",
  "description": "The RabbitMQController is a TypeScript Library that provides a convenient way to manage RabbitMQ connections for Nodejs with features like automatic reconnection and error handling. It extends the `EventEmitter` class to emit events for various connection states.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "build@publish": "tsc && npm publish"
  },
  "repository": {
    "url": "https://github.com/NoErrorStudio/RabbitMQ-Controller"
  },
  "keywords": [
    "rabbitmq",
    "amqplib",
    "rabbitmq-controller",
    "rabbitmq-manager"
  ],
  "author": "achira",
  "type": "module",
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.10.3",
    "axios": "^1.6.0",
    "querystring": "^0.2.1"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.3"
  }
}
